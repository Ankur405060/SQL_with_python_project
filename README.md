# SQL_with_python_project
In our project, we leverage SQL, a fundamental language for interacting with and managing data stored in databases. Specifically, we're using SQLite as our chosen database system. 
